# name: Remote Screenshot Capture
# on:
#   repository_dispatch:
#     types: [capture-hero]

# jobs:
#   screenshot:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'npm'

#       - name: Install Dependencies
#         run: npm ci

#       - name: Take 16:10 Screenshot
#         run: |
#           node -e "
#           const puppeteer = require('puppeteer-extra');
#           const StealthPlugin = require('puppeteer-extra-plugin-stealth');
#           const fs = require('fs');
#           const path = require('path');

#           puppeteer.use(StealthPlugin());

#           (async () => {
#             let browser;
#             try {
#               browser = await puppeteer.launch({ 
#                 executablePath: '/usr/bin/google-chrome',
#                 headless: 'shell',
#                 args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage', '--disable-gpu'] 
#               });
              
#               const page = await browser.newPage();
              
#               await page.setRequestInterception(true);
#               page.on('request', (req) => {
#                 const url = req.url().toLowerCase();
#                 // BLOCKING COOKIE SCRIPTS: Prevents banners from even loading
#                 const isAdsOrConsent = ['google-analytics', 'cookiebot', 'onetrust', 'consent', 'analytics'].some(term => url.includes(term));
                
#                 if (['font', 'media'].includes(req.resourceType()) || isAdsOrConsent) {
#                   req.abort();
#                 } else {
#                   req.continue();
#                 }
#               });

#               await page.setViewport({ width: 1280, height: 800, deviceScaleFactor: 2 });
              
#               await page.goto('${{ github.event.client_payload.url }}', { 
#                 waitUntil: 'networkidle2', 
#                 timeout: 90000 
#               });

#               // --- START COOKIE DISMISSAL LOGIC ---
#               await page.evaluate(() => {
#                 const selectors = [
#                   '#CybotCookiebotDialogBodyLevelButtonLevelOptinAllowallSelection', // Cookiebot (from your screenshot)
#                   '[id*=\"reject\"]', '[id*=\"necessary\"]', '[class*=\"reject\"]', '[class*=\"necessary\"]',
#                   'button[aria-label*=\"Reject\"]', 'button[aria-label*=\"Necessary\"]'
#                 ];
                
#                 selectors.forEach(selector => {
#                   const element = document.querySelector(selector);
#                   if (element) element.click();
#                 });

#                 // Force hide any lingering overlays
#                 const styles = document.createElement('style');
#                 styles.innerHTML = '[class*=\"cookie\"], [id*=\"cookie\"], [class*=\"consent\"], [id*=\"consent\"] { display: none !important; }';
#                 document.head.appendChild(styles);
#               });
#               // --- END COOKIE DISMISSAL LOGIC ---
              
#               await new Promise(r => setTimeout(r, 4000));

#               const dir = path.join(process.cwd(), 'public', 'captures');
#               if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true });
              
#               const fileName = '${{ github.event.client_payload.id }}' + '.webp';
#               const filePath = path.join(dir, fileName);
              
#               await page.screenshot({ path: filePath, type: 'webp', quality: 75, optimizeForSpeed: true });
              
#               console.log('✅ Captured successfully to: ' + filePath);
#             } catch (e) {
#               console.error('❌ Error:', e.message);
#               process.exit(1);
#             } finally {
#               if (browser) await browser.close();
#             }
#           })();"

#       - name: Commit and Push
#         run: |
#           git config --global user.name 'github-actions[bot]'
#           git config --global user.email 'github-actions[bot]@users.noreply.github.com'
#           git add public/captures/
#           git commit -m "Capture: ${{ github.event.client_payload.id }}" || exit 0
#           git pull --rebase origin main
#           git push origin main


name: Remote Screenshot Capture
on:
  repository_dispatch:
    types: [capture-hero]
  schedule:
    # Runs at 00:00 on day 1 of every second month (approx. every 60 days)
    - cron: '0 0 1 */2 *'

jobs:
  screenshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: |
          # Ensuring all necessary scraping and ad-blocking packages are explicitly pulled
          npm ci || npm install puppeteer-extra puppeteer-extra-plugin-stealth @cliqz/adblocker-puppeteer cross-fetch

      - name: Take 16:10 Screenshot
        run: |
          node -e "
          const puppeteer = require('puppeteer-extra');
          const StealthPlugin = require('puppeteer-extra-plugin-stealth');
          const { PuppeteerBlocker } = require('@cliqz/adblocker-puppeteer');
          const fetch = require('cross-fetch');
          const fs = require('fs');
          const path = require('path');

          puppeteer.use(StealthPlugin());

          (async () => {
            let browser;
            try {
              // 1. ABSOLUTE DROP-DEAD TIMER
              // If anything freezes, kill the entire script in 45 seconds max.
              setTimeout(() => { 
                console.error('⏳ Hard script timeout reached. Forcing exit to prevent infinite hang.'); 
                process.exit(1); 
              }, 45000);

              browser = await puppeteer.launch({ 
                executablePath: '/usr/bin/google-chrome',
                headless: 'shell',
                args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage', '--disable-gpu'] 
              });
              
              const page = await browser.newPage();
              
              // Set a strict 25-second max navigation budget
              await page.setDefaultNavigationTimeout(25000);

              // 2. INITIALIZE COMMUNITY AD & COOKIE BLOCKER
              const blocker = await PuppeteerBlocker.fromPrebuiltAdsAndTracking(fetch);
              await blocker.enableBlockingInPage(page);

              await page.setViewport({ width: 1280, height: 800, deviceScaleFactor: 2 });
              
              // Fallback handling for when the 60-day cron runs (which won't have a client_payload)
              const targetUrl = '${{ github.event.client_payload.url }}' || process.env.FALLBACK_URL;
              const fileId = '${{ github.event.client_payload.id }}' || process.env.FALLBACK_ID;

              console.log('🌐 Navigating to:', targetUrl);
              
              // 3. FAST LOADING STATE
              // 'domcontentloaded' finishes immediately when HTML is structural. Won't hang on endless tracking scripts.
              await page.goto(targetUrl, { 
                waitUntil: 'domcontentloaded'
              });

              // 4. INJECT AGGRESSIVE MODAL/BANNER NUKE
              // Forcibly hides structural wrappers and unlocks background scroll freezing
              await page.evaluate(() => {
                const styles = document.createElement('style');
                styles.innerHTML = `
                  [class*=\"cookie\"], [id*=\"cookie\"], [class*=\"consent\"], [id*=\"consent\"],
                  [class*=\"modal\"], [id*=\"modal\"], [class*=\"popup\"], [id*=\"popup\"],
                  .app-banner, .announcement-banner, #onetrust-banner-sdk, #didomi-popup,
                  [id*=\"sp_message_container\"] { 
                    display: none !important; pointer-events: none !important; 
                  }
                  body, html { overflow: auto !important; position: static !important; }
                `;
                document.head.appendChild(styles);
              });
              
              // Give the layout and images exactly 15 seconds to settle visually
              console.log('⏱️ Waiting 15s for visual layout to settle...');
              await new Promise(r => setTimeout(r, 15000));

              const dir = path.join(process.cwd(), 'public', 'captures');
              if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true });
              
              const fileName = fileId + '.webp';
              const filePath = path.join(dir, fileName);
              
              await page.screenshot({ path: filePath, type: 'webp', quality: 75, optimizeForSpeed: true });
              console.log('✅ Captured successfully to: ' + filePath);

            } catch (e) {
              console.error('❌ Error during capture phase:', e.message);
              process.exit(1);
            } finally {
              if (browser) await browser.close();
            }
          })();"
        env:
          # IMPORTANT: Update these default values for your 60-day automated cron runner!
          FALLBACK_URL: 'https://example.com'
          FALLBACK_ID: 'hero-screenshot'

      - name: Commit and Push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add public/captures/
          git commit -m "Capture Update: Auto-renewal [skip ci]" || exit 0
          git pull --rebase origin main
          git push origin main