name: Remote Screenshot Capture
on:
  repository_dispatch:
    types: [capture-hero]

jobs:
  screenshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm' # Uses GitHub's built-in cache for node_modules

      - name: Install Dependencies
        # 'npm ci' is faster than 'npm install' for CI/CD
        run: npm ci

      - name: Take 16:10 Screenshot
        run: |
          node -e "
          const puppeteer = require('puppeteer-extra');
          const StealthPlugin = require('puppeteer-extra-plugin-stealth');
          const fs = require('fs');

          puppeteer.use(StealthPlugin());

          (async () => {
            let browser;
            try {
              browser = await puppeteer.launch({ 
                // OPTIMIZATION: Use the runner's built-in Google Chrome to save 150MB download
                executablePath: '/usr/bin/google-chrome',
                headless: 'shell',
                args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage', '--disable-gpu'] 
              });
              
              const page = await browser.newPage();
              
              // Resource Saving: Block unneeded scripts
              await page.setRequestInterception(true);
              page.on('request', (req) => {
                if (['font', 'media'].includes(req.resourceType()) || req.url().includes('analytics')) {
                  req.abort();
                } else {
                  req.continue();
                }
              });

              await page.setViewport({ width: 1280, height: 800, deviceScaleFactor: 2 });
              
              await page.goto('${{ github.event.client_payload.url }}', { 
                waitUntil: 'networkidle2', 
                timeout: 60000 
              });
              
              // Short wait for Framer animations
              await new Promise(r => setTimeout(r, 4000));

              const dir = 'public/captures';
              if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true });
              
              const path = \`\${dir}/\${{ github.event.client_payload.id }}.png\`;
              
              await page.screenshot({ path: path, optimizeForSpeed: true });
              
              console.log('✅ Captured successfully');
            } catch (e) {
              console.error('❌ Error:', e.message);
              process.exit(1);
            } finally {
              if (browser) await browser.close();
            }
          })();"

      - name: Commit and Push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add public/captures/
          git commit -m "Capture: ${{ github.event.client_payload.id }}" || exit 0
          git pull --rebase origin main
          git push origin main